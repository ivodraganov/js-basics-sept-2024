function tablica(mnojitel) {
    for(let mnojitelEdno = 1; mnojitelEdno <= 10; mnojitelEdno++) {
        let result = mnojitelEdno * mnojitel;
        console.log(`${mnojitelEdno} * ${mnojitel} = ${result}`);
    }
}